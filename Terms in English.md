# **符号的英文读法**

## **1. `-` (Hyphen / Dash)**
- **Hyphen** (`-`) → 连接复合词  
  - 示例: `mother-in-law` → **"mother hyphen in hyphen law"**
- **Dash** (`—` 或 `–`) → 句子中断  
  - 示例: `The result — as expected — was great.`  
    → **"The result dash as expected dash was great."**

---

## **2. `/` (Slash)**
- **Slash** → 用于 **URL、路径**  
  - 示例: `https://openai.com` → **"h t t p s colon slash slash open a i dot com"**
- **Forward slash** → 用于区分 `\` 反斜杠  
  - 示例: `C:/Users/Name/` → **"C colon forward slash Users forward slash Name"**

---

## **3. `,` (Comma)**
- **Comma** → 用于 **列表、数字分隔**
  - 示例: `Apples, oranges, and bananas.` → **"Apples comma oranges comma and bananas."**
  - 示例: `1,000,000` → **"one million"** （有时不会读出 "comma"）

---

## **4. `"` (Quotation mark / Double quote)**
- **Quotation mark / Double quote** → 表示引用或对话  
  - 示例: `"Hello"` → **"double quote hello double quote"**
- **Single quote (`'`)** → 读作 **"single quote"** 或 **"apostrophe"**  
  - 示例: `'It's John's book'` → **"single quote It's John apostrophe s book single quote"**

---

## **总结**
| **符号** | **常见读法** | **示例** |
|---------|------------|----------|
| **`-` (hyphen / dash)** | **Hyphen**（用于连接词）<br>**Dash**（更长的 `—` 用法）<br>**Minus**（数学减号） | `well-known` → "well hyphen known" |
| **`/` (slash)** | **Slash**（最常见）<br>**Forward slash**（用于区分 `\` 反斜杠）<br>**Per**（数学单位） | `http://example.com` → "h t t p colon slash slash example dot com" |
| **`,` (comma)** | **Comma** | `1,000` → "one thousand, comma" |
| **`"` (quotation mark / double quote)** | **Quotation mark** / **Double quote** | `"Hello"` → "double quote hello double quote" |

🚀 **这些读法在编程、数学、语言表达中都很常见！**
